const title = 'pnpm';
const isEnabled = ({ manifest }) => Boolean(manifest.packageManager?.startsWith('pnpm@'));
const config = ['.pnpmfile.cjs'];
export default {
    title,
    isEnabled,
    config,
};
